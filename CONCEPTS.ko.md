# libnhttp — 설계 개념

**Language**: [English](CONCEPTS.md) | 한국어

이 문서는 원래 libnhttp 구현의 **설계 철학과 불변조건들**을 처음부터 다시 만드는 재설계 작업을
위해 추출해 정리한 것입니다. 폐기되는 구현 세부사항(스레드풀 내부 동작, 청크 버퍼 할당자 등)은,
그 뒤에 있는 *아이디어*가 *코드*는 사라지더라도 살아남아야 하는 경우를 제외하고는 의도적으로
다루지 않습니다.

## 1. 핵심 철학

- **이벤트 드리븐, 리스너당 하나의 리액터.** 하나의 `epoll`(또는 플랫폼 등가물) 루프가 여기에
  등록된 모든 소켓을 구동합니다. 소켓의 준비 상태(readiness)를 건드릴 수 있는 것은 리액터
  루프뿐입니다; 그 외 모든 것(파싱, 핸들러 실행)은 옆으로 빠져서 처리되고 논블로킹 폴을 통해
  결과를 돌려줄 뿐, 리액터 스레드 자체를 블로킹하는 일은 절대 없습니다.
- **스트리밍이 우선.** 요청 본문과 응답 본문은 버퍼링된 덩어리가 아니라 `stream` 객체입니다.
  응답은 파일, 파일의 바이트 범위, 메모리, 또는 프로그램적으로 생성되는 소스 등 무엇으로든
  뒷받침될 수 있습니다 — 와이어 계층은 신경 쓰지 않고 그저 `read`/`get_length`/`is_nonblock`만
  필요로 합니다.
- **계층화된 요청/응답 모델.** 다음과 같이 명확히 분리됩니다:
  - **raw 계층**: 와이어에서 온 바이트 → `메서드 + 경로 + 헤더 + 본문 스트림`, 라우팅이나 가상
    호스트, REST 관례에 대한 어떤 견해도 갖지 않음.
  - **파사드 계층**: raw 계층을 사용자 코드에 넘겨줄 어법 좋은 request/response 객체로 감싼
    것 — 확장, vhost, 라우터가 여기에 연결됩니다.
  이 분리 덕분에 프로토콜 업그레이드(WebSocket)와 미래의 프로토콜들이 raw
  accept/parse 루프가 그 존재를 몰라도 끼워질 수 있습니다.
- **끼워 넣을 수 있는 모든 것은 우선순위 순서로 시도되는 체인이다.** 리스너 레벨 확장(vhost,
  정적 overlay, 라우터, websocket 핸드셰이크, 사용자 확장)은 모두 동일한 두 단계 계약 —
  "이 요청을 원하는가?" 그다음 "처리하라" — 을 구현하며, 우선순위 순서로 시도되어 거부되면
  다음으로 넘어갑니다. 이것이 가상 호스팅, 경로 스코핑, 정적 파일 서빙, REST 라우팅 모두에
  쓰이는 유일한 합성(composition) 메커니즘입니다; 이들 중 어느 것도 리스너 자체에 특수 케이스로
  박혀 있지 않습니다.
- **편의보다 유계 자원 사용.** 버퍼는 커넥션마다 무한정 커지는 대신 풀링/상한이 걸려 있으며,
  풀이 소진되면 모든 커넥션의 메모리 예산을 저하시키는 대신 새 커넥션을 그냥 거부합니다.
  재설계판은 다른 메커니즘(예: 슬랩 할당자 대신 커넥션별 할당 + 전역 커넥션 수 상한)을 선택할
  수 있지만 그 *보장*만은 유지해야 합니다: 진행 중인 커넥션들이 사용하는 총 메모리는 트래픽이
  아니라 설정된 한도로 유계되어야 합니다.
- **플루언트하게 체이닝 가능한 설정 API.** 라우트/확장 등록은 하나의 문장처럼 읽힙니다:
  `router->get("path", target)->post(...)->group([...]){...}`. `this_ptr<T>`를 대체하는
  어떤 메커니즘이든, 모든 중간 객체를 `shared_ptr` 소유로 강제할 필요가 없다면 그렇게 하지
  않으면서 이 어법적 표면을 보존해야 합니다.
- **태그 기반의 확장 가능한 커넥션/요청 단위 메타데이터.** 모든 확장이 공유 컨텍스트 객체에
  필드를 추가하는 대신, 각 확장은 커넥션(`http_link`)이나 요청에 자기 자신만의 강타입 "태그"를
  (타입으로 조회) 붙입니다. 이것이 vhost 스태킹, 경로 스코핑, 라우트 캡처 상태가 하나의
  거대 객체 없이도 공존할 수 있게 해주는 방법입니다. **커넥션 단위** 태그(vhost/vpath 스코프
  스택 — keep-alive 커넥션에서 요청들 사이에도 유지됨)와 **요청 단위** 태그(라우트 매치 상태,
  미들웨어 체인 위치 — 요청마다 리셋됨) 사이의 생애주기 구분은 우연이 아니라 설계상 반드시
  지켜야 하는 선택이며, 새 설계에서도 (하나의 태그 스코프로 뭉뚱그리지 말고) 명시적으로
  보존되어야 합니다.

## 2. 동시성 모델 (재설계 대상이지만, 계약은 반드시 지켜져야 함)

원래 모델: 고정된 스레드풀이 블로킹 작업 단위(파싱, 헤더 직렬화, 블로킹 본문 읽기)를
실행하고; epoll 스레드는 절대 블로킹하지 않습니다 — 틱마다 한 번 "이거 끝났나?"를 폴링하고
백그라운드 결과가 준비될 때까지 같은 상태에 재진입합니다. 이것은 C++17에 진짜 비동기 I/O
프리미티브가 없었기 때문에 택한 우회법입니다.

**재설계에 어떤 동시성 프리미티브를 택하든(폴링을 곁들인 스레드풀 vs. C++20 코루틴 vs. 그 외
무엇이든) 반드시 보존되어야 하는 것:**
- 리액터 스레드는 워커를 기다리며 뮤텍스/condvar/read에서 절대 블로킹하면 안 된다.
- CPU 바운드거나 파일시스템 블로킹 작업(stat, 논블로킹이 아닌 스트림에서의 파일 읽기, 큰 본문의
  문자열 파싱)은 같은 리액터를 공유하는 다른 커넥션들을 정체시켜서는 안 된다.
- 커넥션의 프로토콜 상태 머신은 비동기 단계가 끝난 뒤 멈췄던 곳에서 재개 가능해야 한다 — 즉
  "내가 지금 어디에 있는지"는 콜스택이 아니라 상태 머신이 소유한다.

C++20을 겨냥한 재설계라면 블로킹-태스크+폴 관용구를 **스택리스 코루틴**(소켓 준비 상태나
future에 대한 `co_await`)으로 대체하는 것을 진지하게 고려해야 합니다 — 같은 논블로킹 보장을
유지하면서도 원래 코드의 복잡성 한 부류(상태 enum, "AGAIN vs RETRY vs SUCCESS" 스텝 결과)를
통째로 제거해주기 때문입니다. 이는 구현 전에 확정해야 할 아키텍처적 결정입니다(첨부된 계획
참고).

## 3. 프로토콜 계층 관례

- **점진적(incremental) 파싱.** 모든 프로토콜 요소(요청 라인, 헤더 라인, 청크 헤더, MIME
  타입)는 부분 버퍼를 넘겨받을 수 있고 "더 필요함"과 "형식 오류"를 구분해서 보고하는 함수로
  파싱됩니다. 이건 리액터 모델을 가능하게 하는 것이므로 반드시 살아남아야 합니다 — 파싱은
  전체 요청이 이미 메모리에 있다고 절대 가정하지 않습니다.
- **할당 없는 잘 알려진 상수.** 컴파일 타임에 알려진 헤더 이름, 메서드, mime 타입, 상태
  문구는 힙 할당이나 `std::string` 생성 없이 비교됩니다(재설계판에서는 `string_view` 형태의
  비교). 진짜로 동적인 값(커스텀 헤더 이름, 호출자가 제공한 경로)만이 할당자를 건드립니다.
- **스위치문이 아니라 데이터로서의 메서드 시맨틱.** 어떤 메서드가 관례적으로 요청 본문을
  가지는지, 멱등(idempotent)인지, 캐시 가능한지는 호출부마다 하드코딩하는 게 아니라 메서드별
  작은 플래그 테이블에서 조회됩니다 — 새로운/커스텀 메서드도 같은 시맨틱을 가질 수 있습니다.
- **스트림은 출처와 무관하게 스트림이다.** 파일 본문, 인메모리 본문, 그리고 둘 중 무엇의
  바이트 범위 하위 뷰든 모두 그냥 `stream` 구현체입니다. Range 요청은 콘텐츠 소스마다
  중복되는 별도 코드 경로가 아니라 내부 스트림 위의 *데코레이터*입니다. 조건부 GET
  (ETag / If-Modified-Since / If-Range)과 Range 처리는 "고정된 파일 하나 서빙"과 "디렉터리
  아래 파일들 서빙" 확장 둘 다에서 쓰이는 공유 로직입니다 — 재설계판에서 이 둘 사이에 이 로직을
  중복시키지 마십시오.
- **경로 정규화는 중앙화되어 있다.** URL 경로 정규화(`.`/`..` 해석, 중복 슬래시 제거)는 하나의
  공유 유틸리티를 통해 이루어지며, 라우터와 정적 파일 overlay, 경로 스코핑 확장이 동일하게
  사용합니다. 한 번 고친 경로 순회(traversal) 버그는 쓰이는 모든 곳에서 고쳐져야 합니다 —
  이는 구현체가 정확히 하나뿐일 때만 가능합니다.

## 4. 확장/합성 모델

- **두 단계 계약**: `wants(request) -> bool`, 그다음 `handle(request) -> response`.
  레지스트리 전체에 대해 오름차순 우선순위로 평가되며, 처음 받아들이는 쪽이 승리하고, 처리되지
  않으면 기본(404/501 형태) 응답으로 폴스루합니다.
- **합성 가능한 스코핑 확장들.** 일부 확장(vhost, 경로-프리픽스 스코핑)은 스스로 체인을
  종료하지 않습니다 — 그것들은 자신이 소유한 *중첩된* 확장/라우트 레지스트리를 위해 컨텍스트
  (현재 호스트, 남은 경로)를 좁히고, 끝나면 그 좁힘을 정리합니다(중첩 디스패치를 감싸는
  `enter`/`leave`). 라우터는 순수 가상 경로 마운팅에 쓰이는 것과 *같은* 경로 스코핑
  프리미티브 *위에 구축*됩니다 — 별도의 메커니즘이 아닙니다.
- **사용자가 등록하는 모든 것은 terminal(응답을 만들어냄)이거나 scope(좁히고 위임함) 중
  하나다.** 하나의 인터페이스가 둘 다 하려는 것이 아니라 이 구분을 명시적으로 유지하는 것이,
  원래 라우터가 프리픽스 매칭을 재구현하는 대신 vhost/vpath 메커니즘을 재사용할 수 있게 해준
  이유입니다.

## 5. 정확히 보존해야 할 라우팅(xfwk) 시맨틱

- **경로 세그먼트에 대한 트라이**이며 평평한 패턴 목록이 아닙니다 — 경로 세그먼트당 노드 하나,
  네 가지 종류: static(정확한 이름), parameter(`:name`, 선택적 predicate로 필터링됨),
  wildcard(나머지 전체를 잡는 catch-all, 자식을 가질 수 없음), root(합성된 최상위 노드).
- **세그먼트별 매칭 우선순위, 순서대로**: 정확한 static 이름 자식이 항상 어떤 parameter
  자식보다 먼저 시도된다; predicate가 세그먼트를 받아들이는 여러 parameter 자식들 중에서는,
  서브트리 매치가 경로를 *가장 깊게* 소비하는 쪽이 이긴다(단순히 처음 통과한 predicate가
  아님); static도 어떤 parameter 브랜치도 완전한 매치에 도달하지 못했을 때만 wildcard
  catch-all이 적용된다. **이 정확한 우선순위 결정 규칙은 예전에 실제로 출시된 버그의
  원인이었습니다**(순진하게 "처음 성공한 param 매치가 이긴다"는 방식은, 백트래킹 시 더 얕은
  param 매치가 더 깊고 구체적인 매치를 밀어내는 결과를 낳았습니다) — 재설계판의 테스트
  스위트는 형제 관계인 static/param/wildcard 라우트와 같은 세그먼트에 여러 param 후보가 있는
  회귀 테스트를 반드시 포함해서 이 동작을 고정해야 합니다.
- **캡처된 파라미터는 실패한 백트래킹 시도를 가로질러 새어나가면 안 된다.** parameter
  브랜치가 시도되었다가 트리의 더 깊은 곳에서 결국 실패한다면, 다음 형제가 시도되기 전에 그
  캡처는 롤백되어야 합니다.
- **라우트 등록은 HTTP 메서드별 플루언트 DSL이다**(`get/post/put/patch/delete/any`). 같은
  경로에 두 번째 메서드가 등록되는 순간 "이 경로에 대한 타겟 하나"에서 "이 경로에 대한
  메서드별 디스패치 테이블"로 자동 승격됩니다 — 호출자는 "이 경로는 여러 메서드를 가질
  것이다"를 미리 선언할 필요가 절대 없어야 합니다.
- **미들웨어는 타겟 주위에 책임 사슬(chain-of-responsibility)로 합성된다**, 각 미들웨어는
  다음 링크를 호출할지/어떻게 호출할지 결정하며 최종적으로 타겟에서 끝납니다. 미들웨어는 단일
  라우트에 붙을 수도, 함께 등록된 라우트들의 *그룹*에 붙을 수도 있습니다(라우트 등록 호출들을
  묶어서 이후의 미들웨어 부착이 한꺼번에 전부에 적용되게 하는 "그룹핑" API) — 이 그룹핑
  어법은 README/테스트 스타일의 라우트 등록에서 많이 쓰이므로 보존할 가치가 있습니다.

## 6. 원래 구현에 있던 알려진 공백들 (이것들을 조용히 "보존"하지 말고 — 재설계판이 고칠지
   여부를 명시적으로 결정할 것)

- **Multipart form-data(`multipart/form-data`) 파싱은 구현된 적이 없습니다** — 데이터를
  담는 형태만 존재했고, 실제 파서는 없었습니다. `nvalue.hpp`의 범용 값 트리는 거의 전적으로
  이 미완성 기능을 위해 존재하며 그 외에는 죽은 코드입니다.
- **WebSocket 데이터 프레임 프로토콜은 구현된 적이 없습니다** — HTTP Upgrade 핸드셰이크(101
  Switching Protocols, `Sec-WebSocket-Accept` 계산)만 동작했습니다; 프레임
  인코딩/디코딩/마스킹/조각화, `send`/`close`/`on_message`는 스텁이었습니다. README는
  WebSocket 지원을 홍보했지만, 구현은 이를 제공하지 못했습니다.
- **TLS/SSL 미지원** — HTTP/1.1 평문만 지원(원래 README에서 향후 항목으로 명시적으로 언급됨).
- **플랫폼 매크로 이름/동작 함정**: 원래 헤더들은 MSVC 아래에서 `inline` 키워드 자체를
  재정의했습니다(`#define inline __forceinline`) — 재설계판에서는 그대로 가져오지 말고
  의도적으로 피해야 할 위험 요소입니다.
- **죽은 빌드 익스포트 장치**: DLL 익스포트 매크로(`NHTTP_API`)가 존재했지만, 실제로 익스포트
  경로를 사용하는 공유 라이브러리를 만들어낸 빌드 구성은 하나도 없었습니다 — 실제로는 항상
  정적 링크되었습니다.

## 7. 네이밍 메모

원래 HAL 뮤텍스 래퍼는 일반적인 상호 배제(mutual exclusion)를 구현했을 뿐인데도 "barrier"
(`barrior_t`, 오타 그대로)라는 이름이 붙어 있었습니다 — 동기화 배리어가 아닙니다. 재설계판은
프리미티브를 실제로 하는 일에 따라 이름 지어야 합니다(`barrier`가 아니라 `mutex`), 이런 식의
혼란스러운 레거시 이름을 피하기 위해서입니다.
