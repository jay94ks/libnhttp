# 성능 개선 계획

**언어**: [English](PLAN.md) | 한국어

이 저장소의 성능 관련 후속 작업을 위한 살아있는 할 일 목록입니다 — **아직 남아 있는 항목만**
담습니다. 완료된 작업은 끝나는 대로 여기서 지워집니다; 무엇을 시도했고, 무엇을 측정했고,
무엇을 왜 반려했는지에 대한 전체 이야기는 `CLAUDE.md`의 phase 로그에 영구적으로 남아 있고,
핵심 수치는 `ReadMe.md`의 벤치마크 섹션에 있습니다. 이력이 궁금하면 그것부터 확인하세요 —
이 파일은 항상 "지금 남은 것"만 담습니다.

## 남은 작업

- **Windows: 정적 파일 응답을 위한 `sendfile(2)` 대응물.** Linux 고속 경로(`CLAUDE.md`의
  Phase 16 로그 참고)는 아직 Windows 대응물이 없습니다 — `platform::socket_handle::
  supports_send_file()`는 거기서 `false`를 반환해서, 모든 응답이 일반 read/write 경로로
  폴백합니다. `TransmitFile`이 자연스러운 선택이지만, 이 리액터의 평범한 읽기/쓰기가 지금
  쓰지 않는 진짜 오버랩드 I/O 완료 처리가 필요합니다; 대충 얹는 게 아니라 그게 설계되고
  검증 가능해진 뒤에 하세요.
- **실제로 `perf`를 돌릴 수 있는 호스트에서의 프로파일링.** 이 WSL2 커널에는 맞는
  `linux-tools` 패키지가 없고, `strace -c`는 그 대체재로 쓰기엔 너무 침습적입니다
  (`CLAUDE.md`의 Phase 16 로그 참고). 그 phase의 몇 가지 질문은 대신 직접 A/B 벤치마크로
  해결했는데, 이건 유효한 증거이긴 하지만 실제 프로파일보다는 거칠습니다 — 이 영역에서 더
  미세한 최적화를 원한다면 네이티브 리눅스 호스트에서 `perf`로 다시 살펴볼 가치가 있습니다:
  코루틴 프레임 할당 처리량(같은 로그의 P3, 측정된 개선이 없어서 반려됨)과 "워커를 과다
  프로비저닝하지 말 것" 이상의 더 세밀한 `thread_pool` 튜닝(같은 로그의 P4).
- **`router::route_match()`의 후보별 백트래킹 비용.** 지금까지(Phase 16) 벤치마크한 건 전부
  `overlay`의 정적 파일 경로입니다 — `router` 모듈은 이 수치들 어디에서도 부하를 받아본 적이
  없는데, 코드를 살펴보니 손대기 전에 제대로 측정할 가치가 있는 구체적이고 그럴듯한
  오버헤드가 이미 보입니다:
  - `route_state`(`include/nhttp/router/route.hpp`)는 캡처를 `std::map<std::string,
    std::string>`(레드-블랙 트리)에 담습니다 — 그래서 `route_state trial = state;`(정적 자식
    시도 한 번마다, 그리고 파라미터 자식 루프의 후보 하나마다 — 즉 최종 매치당 한 번이 아니라
    탐색 중 방문하는 트리 노드마다 — `route::route_match()` 참고)가 매번 새 노드 할당
    세트를 만드는데, 그중 대부분은 백트래킹으로 버려지는 가지를 위한 것입니다. 경로 파라미터가
    하나나 둘만 있는 라우트도 승리한 경로뿐 아니라 탐색의 매 세그먼트마다 이 비용을 치를
    가능성이 있습니다.
  - `route::route_match()`는 `p->predicate_(std::string(segment))`를 호출합니다 — 세그먼트당
    파라미터 자식 후보마다 할당하는 `std::string` 생성인데, 실제로는 소유권이 전혀 필요 없는
    predicate 시그니처(`std::function<bool(const std::string&)>`)를 만족시키기 위한 것뿐입니다
    (`USAGE.md`/예제 앱의 실제 predicate는 전부 동등 비교만 합니다).
  - 아마 영향은 더 작겠지만 이 코드를 만지는 김에 확인할 가치가 있는 것: `route::
    method_targets_`는 메서드 *이름 문자열*로 키를 잡은 `std::map<std::string, target_ptr>`
    이고, 매치된 요청당 `get_target()`으로 한 번만 조회됩니다 — 요청당 O(1)이라 위의 것보다는
    저렴하지만(방문 노드마다가 아님), 그래도 `protocol::http_method`가 이미 더 저렴한
    식별자를 갖고 있는데 문자열 비교 트리 조회를 하는 셈입니다.
  - **뭔가를 바꾸기 전에**: 이 저장소의 벤치마크 스위트(`benchmark/docker/`,
    `bench_server.cpp` 스타일 하네스)는 `overlay`만 사용해서, 라우팅된 요청에 대한 측정
    기준선이 아예 없습니다. 벤치마크 방법론을 라우터 기반 엔드포인트(예: 이 코드베이스 자신의
    `:user/profile` 스타일 예제처럼 경로 파라미터 하나나 둘을 가진 라우트)로 *먼저*
    확장해서 기준선을 세우고, 그다음 후보 수정(캡처를 `std::map` 대신 작은
    `std::vector<std::pair<...>>`로; predicate가 `std::string_view`를 받도록)을 하나씩
    적용하며 그 기준선에 대해 A/B 비교하세요 — Phase 16의 P3와 P4가 이미 필요하다고 검증한
    바로 그 방법론입니다(둘 다 종이 위에서는 확실한 개선처럼 보였지만, 하나만 실제 개선으로
    측정됐고 둘은 아니어서 반려됐습니다).

## 이 계획에서 명시적으로 제외되는 것

- 이미 별도로 추적 중인 것들: QUIC/HTTP-3(`CLAUDE.md`의 아키텍처 결정 8번 참고),
  TLS 위 ALPN 협상 HTTP/2, HTTP/2 `CONTINUATION`/서버 푸시(`CLAUDE.md`의 Phase 14 로그 참고).
- 리버스 프록시 커넥션 풀링과 능동적 업스트림 헬스체크(`CLAUDE.md`의 Phase 13 로그가 이미 이번
  라운드 범위 밖의 알려진, 합리적인 후속 작업으로 기록해둠).
- `io_context`/`async_socket`을 현재의 이식 가능한 준비성(readiness) 기반 계약 대신 네이티브
  완료 모델 중심으로 다시 짜는 것(`CLAUDE.md`의 Phase 12 설계 노트가 Windows/IOCP에서도 왜
  일부러 이걸 피했는지 설명함) — 여기 어떤 항목도 그걸 재검토할 필요가 없습니다.
