# 성능 개선 계획

**언어**: [English](PLAN.md) | 한국어

`ReadMe.md`의 벤치마크 섹션에 기록된 벤치마크 라운드 이후 작성. 그 벤치마크에서 nhttpd는 지속
부하 아래 작은 정적 파일을 서빙할 때 nginx와 Apache에 상당히 뒤처지는 것으로 측정되었습니다
(루프백: 13.8K req/s 대 nginx의 139K, Apache의 38.6K; Docker 네트워크 스택 벤치마크에서도 같은
상대적 격차). 그 라운드에서 발견된 실제 정합성 버그 두 개(`task<T>` 코루틴 프레임의 구조적 누수와
`SIGPIPE` 크래시)는 이미 `main`에 수정되어 있습니다 — 전체 이야기는 `ReadMe.md`와 `CLAUDE.md`의
Phase 15 로그 참고. 이 문서는 그 수정 이후에도 남아있는 처리량 격차에 관한 것이며, 이건 버그가
아니라 진짜 아키텍처/구현상의 격차입니다.

아래 각 항목은 범위가 명확하고, 기대 효과 대비 노력 순으로 정렬되어 있으며, `benchmark/docker/`의
스택(앞으로의 표준 재현 가능 벤치마크)을 다시 돌려 검증하고 `ReadMe.md`의 벤치마크 섹션을 새
수치로 갱신해야 합니다 — `CLAUDE.md`의 각 phase가 "빨라질 것이다"가 아니라 실제 측정된 검증으로
마무리되는 것과 같은 방식입니다.

## P0 — 완료 (맥락 설명, 추가 작업 아님)

- [x] `task<T>` 코루틴 프레임 누수 수정 (`include/nhttp/async/task.hpp`).
- [x] `listener`의 생성자에서 `SIGPIPE` 무시 — 죽어가는 클라이언트 하나가 프로세스를 죽이지
  못하도록.

## P1 — 정적 파일을 위한 `sendfile` 기반 고속 경로

**왜 이게 1순위인가**: 알려진 격차 중 가장 큽니다. nginx는 정적 GET을 `sendfile()` 시스템콜
하나로 처리합니다 — 유저스페이스 복사 없음, 스레드 전환 없음. nhttpd의 현재 경로
(`overlay` → `file_stream`/`range_stream` → `http1_io::write_message_body`)는 각 4&nbsp;KB
청크를 블로킹 스레드 풀에서 유저스페이스 버퍼로 읽은 뒤, 그 버퍼를 리액터 스레드에서 다시
써냅니다 — 청크마다 복사 두 번과 스레드 전환 한 번, 커널이 페이지 캐시에서 소켓으로 직접 옮길
수도 있었던 데이터를 위해.

**계획**:
- `static_content.cpp`/`connection.cpp`의 응답 쓰기 경로에 좁은 범위의 고속 경로 추가: 응답
  바디가 순수 `file_stream`으로 뒷받침되고(`Range` 없음, chunked/transfer coding 없음, 연결에
  TLS 없음 — TLS는 커널 수준 sendfile 대응물이 없음) 플랫폼이 지원하면, 일반적인
  `read`/`write` 루프 대신 파일 디스크립터에서 소켓 디스크립터로 바로 `sendfile(2)`(Linux)를
  사용.
- Windows 대응물: `TransmitFile` — 이 코드베이스의 IOCP 기반 리액터(오버랩드, 이벤트 기반)와
  이미 잘 맞는 조합이며, `CLAUDE.md`의 Phase 12 작업과 자연스럽게 짝을 이룹니다.
- 기존 일반 경로는 TLS 연결, 바이트 `Range` 요청(부분 파일 sendfile도 syscall의 offset/count
  인자로 가능하니, 단순한 경우가 되고 나면 해볼 가치가 있음), 파일이 아닌 스트림
  (`reverse_proxy`, 생성된 응답 등)의 폴백으로 유지 — 이건 `io::stream`의 일반 계약을
  대체하는 게 아니라 추가하는 것입니다.
- 이것만으로도 측정된 특정 마이크로벤치마크(작은 정적 파일 반복)의 격차 대부분이 줄어들 것으로
  예상 — 아래 항목들을 하기 전에 다시 측정할 가치가 있습니다.

## P2 — 요청당 불필요한 스레드 풀 왕복 줄이기

P1과 독립적(sendfile 대상이 아닌 응답, 그리고 모든 응답에 선행하는 메타데이터 작업에는 여전히
필요):

- `overlay::wants()`와 `overlay::handle()` 둘 다 `resolve()`를 호출하는데, 이게 같은 경로를
  독립적으로 `stat()`합니다 — `CLAUDE.md`의 Phase 8에서 이미 기록된 트레이드오프("아직 이
  규모에서 최적화할 가치는 없음 ... 요청-태그 캐시가 필요할 것")입니다. 이제는 그럴 가치가
  있습니다: 해석된 `(fs_path, file_info)`를 요청의 태그 저장소에 캐시해서(`CONCEPTS.md`의 태그
  메커니즘 참고) `wants()`가 이미 처리했다고 표시한 요청이 `handle()`에 도달했을 때 파일시스템을
  두 번 stat하지 않도록.
- `file_stream::open()`은 별도의 `pool_->run()` 왕복을 두 번 합니다 — 하나는 `fopen`,
  하나는 초기 `ftell`/`fseek`/`ftell` 크기 탐지. 파일을 열고 크기를 재는 걸 스레드 풀 작업
  하나로 합치면 모든 파일 오픈의 스레드 풀 왕복이 절반으로 줄어듭니다.
- `overlay`의 `stat_file()` 결과와 `file_stream::open()` 자체의 크기 탐지를 아예 스레드 풀
  왕복 하나로 공유할 수 있는지도 검토 — 세 번째 왕복을 없앨 수 있지만, `resolve()`와 실제 파일
  읽기 사이에 크기가 바뀌지 않는다고 신뢰해야 하는 대가가 있습니다(허용 가능: 기존 코드도
  이미 같은 레이스를 암묵적으로 갖고 있습니다 — `resolve()`와 실제 읽기 사이에 크기를 재검증하는
  로직이 원래 없습니다).

## P3 — 코루틴 프레임 할당 처리량(churn) 줄이기

P0 누수 수정 후엔 프레임이 올바르게 해제되긴 하지만, 여전히 모든 `co_await`가 콜리 프레임을
위해 새로 힙 할당을 합니다(표준 `operator new` 코루틴 할당 경로). 요청 하나가 `task<T>`를
반환하는 호출을 수십 번 연쇄하므로, 누수와는 별개로 지속 부하에서 실제로 할당자 부담이 됩니다.

- 먼저 프로파일링(추측이 아니라 실제 데이터가 필요합니다 — Docker 벤치마크 부하 아래
  `perf record`/`perf report`를 돌려보면 할당자 시간이 P1/P2가 노리는 스레드 풀 왕복 대비
  실제로 유의미한지 알 수 있습니다).
- 유의미하다면: `task_promise<T>`/`task_promise<void>`에 크기별 free-list 할당자를 뒷받침하는
  자체 `operator new`/`delete`를 줄 수 있습니다(잘 알려진 코루틴 최적화 기법입니다 — 특정
  코루틴 함수 인스턴스화의 프레임 크기는 고정이므로, 범용 할당자가 아니라 단순한 크기별
  free list로 충분합니다). `include/nhttp/async/task.hpp`에만 국한된 변경입니다.

## P4 — `thread_pool`의 큐가 병목이 되면 재검토

`async::thread_pool`(`src/async/thread_pool.cpp`)은 모든 워커 스레드와 모든 제출 코루틴이
공유하는 단일 `std::mutex` + 단일 `std::condition_variable` + 단일 `std::queue`입니다. 실제
부하에서 의미 있는 수준의 `blocking_pool_size`(이번 벤치마크 라운드에서는 64 사용)에서는 이
뮤텍스 하나에 경합이 생길 법하지만, **아직 측정되지 않았습니다** — P1/P2 대비 실제로 병목임을
보여주는 프로파일링 데이터 없이는 건드리지 마세요. 샤딩되거나 락-프리인 큐는 지금은 단순하고
올바른 컴포넌트에 들여올 만한 실제 복잡도이기 때문입니다.

## 이 계획에서 명시적으로 제외되는 것

- 이미 별도로 추적 중인 것들: QUIC/HTTP-3(`CLAUDE.md`의 아키텍처 결정 8번 참고),
  TLS 위 ALPN 협상 HTTP/2, HTTP/2 `CONTINUATION`/서버 푸시(`CLAUDE.md`의 Phase 14 로그 참고).
- 리버스 프록시 커넥션 풀링과 능동적 업스트림 헬스체크(`CLAUDE.md`의 Phase 13 로그가 이미 이번
  라운드 범위 밖의 알려진, 합리적인 후속 작업으로 기록해둠).
- `io_context`/`async_socket`을 현재의 이식 가능한 준비성(readiness) 기반 계약 대신 네이티브
  완료 모델 중심으로 다시 짜는 것(`CLAUDE.md`의 Phase 12 설계 노트가 Windows/IOCP에서도 왜
  일부러 이걸 피했는지 설명함) — 여기 어떤 항목도 그걸 재검토할 필요가 없습니다.
