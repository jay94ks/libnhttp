# 성능 개선 계획

**언어**: [English](PLAN.md) | 한국어

`ReadMe.md`의 벤치마크 섹션에 기록된 벤치마크 라운드 이후 작성. 그 벤치마크에서 nhttpd는 지속
부하 아래 작은 정적 파일을 서빙할 때 nginx와 Apache에 상당히 뒤처지는 것으로 측정되었습니다
(루프백: 13.8K req/s 대 nginx의 139K, Apache의 38.6K; Docker 네트워크 스택 벤치마크에서도 같은
상대적 격차). 그 라운드에서 발견된 실제 정합성 버그 두 개(`task<T>` 코루틴 프레임의 구조적 누수와
`SIGPIPE` 크래시)는 이미 `main`에 수정되어 있습니다 — 전체 이야기는 `ReadMe.md`와 `CLAUDE.md`의
Phase 15 로그 참고. 이 문서는 그 수정 이후에도 남아있는 처리량 격차에 관한 것이며, 이건 버그가
아니라 진짜 아키텍처/구현상의 격차입니다.

아래 각 항목은 범위가 명확하고, 기대 효과 대비 노력 순으로 정렬되어 있으며, `benchmark/docker/`의
스택(앞으로의 표준 재현 가능 벤치마크)을 다시 돌려 검증하고 `ReadMe.md`의 벤치마크 섹션을 새
수치로 갱신해야 합니다 — `CLAUDE.md`의 각 phase가 "빨라질 것이다"가 아니라 실제 측정된 검증으로
마무리되는 것과 같은 방식입니다.

**현황: P0~P5 전부 완료.** 종합 결과, 루프백: 13.8K → 58.0K req/s(약 4.2배); Docker 네트워크
스택 벤치마크: 11.2K → 35.9K req/s(약 3.2배) — nhttpd는 이제 둘 다에서 Apache를 확실히 앞서고
nginx 처리량의 대략 절반(루프백)에서 3분의 2(Docker) 수준까지 왔습니다(예전엔 각각 10분의 1,
6분의 1 수준). 두 항목(P3, 그리고 P4의 처음 두 번의 반복)은 이론만으로 판단하지 않고
구현·측정한 뒤 반려했습니다 — 무엇을 시도했고 왜 유지되지 않았는지는 아래 각 항목을 참고하세요;
이건 낭비된 노력이 아니라 위에서 말한 "검증되어야 한다"가 실제로 의미하는 바입니다.

## P0 — 완료 (맥락 설명, 추가 작업 아님)

- [x] `task<T>` 코루틴 프레임 누수 수정 (`include/nhttp/async/task.hpp`).
- [x] `listener`의 생성자에서 `SIGPIPE` 무시 — 죽어가는 클라이언트 하나가 프로세스를 죽이지
  못하도록.

## P1 — 정적 파일을 위한 `sendfile` 기반 고속 경로 — 완료, 측정됨

**왜 이게 1순위였나**: 알려진 격차 중 가장 컸습니다. nginx는 정적 GET을 `sendfile()` 시스템콜
하나로 처리합니다 — 유저스페이스 복사 없음, 스레드 전환 없음. nhttpd의 예전 경로
(`overlay` → `file_stream`/`range_stream` → `http1_io::write_message_body`)는 각 4&nbsp;KB
청크를 블로킹 스레드 풀에서 유저스페이스 버퍼로 읽은 뒤, 그 버퍼를 리액터 스레드에서 다시
써냈습니다 — 청크마다 복사 두 번과 스레드 전환 한 번, 커널이 페이지 캐시에서 소켓으로 직접
옮길 수도 있었던 데이터를 위해.

**실제로 반영된 내용**: `http1_io::write_message_body`(`src/server/http1_io.cpp`)는 이제
응답 바디가 순수 `io::file_stream`으로 뒷받침되고(`dynamic_cast`로 확인 — 바이트 `Range`
응답의 `file_stream`은 `range_stream`으로 감싸져 있어 이 캐스트가 실패해서 올바르게 일반
경로로 넘어갑니다), 와이어가 순수 `io::socket_stream`이며(같은 방식으로 확인 — `tls_stream`으로
감싼 연결도 올바르게 일반 경로로 넘어갑니다, TLS는 커널 수준 sendfile 대응물이 없으므로),
`content_length`가 알려진 양수일 때 순수 POSIX `sendfile(2)`로 파일 디스크립터에서 소켓
디스크립터로 바로 보내는 고속 경로를 탑니다. 이를 위해 `io::file_stream`은 `native_fd()`를,
`platform::socket_handle`은 `send_file()` + `supports_send_file()`(이미 `set_reuse_port()`가
쓰던 "추측이 아니라 실제 기능 확인" 패턴과 동일)을, `async_socket`은 `write_some()`과 완전히
동일한 would-block/재시도 계약을 쓰는 `send_file()` 코루틴을 얻었습니다. **Windows
(`TransmitFile`)는 이번 라운드에 일부러 시도하지 않았습니다** — 게을러서가 아니라 실제
위험 때문입니다: `OVERLAPPED` 없는 `TransmitFile`은 블로킹되거나, 이 리액터의 평범한 읽기/쓰기가
일부러 쓰지 않는 진짜 IOCP 완료 모델이 필요합니다(`CLAUDE.md`의 Phase 12 설계 노트 참고);
이 코드베이스의 다른 모든 것만큼 철저하게 오버랩드 완료의 엣지 케이스를 검증할 수 없는 채로
끼워 넣으면 뭔가 미묘하게 잘못된 걸 배포할 위험이 있었습니다. `platform::socket_handle::
supports_send_file()`은 Windows에서 `false`를 반환하므로, 그쪽의 모든 응답은 여전히 (완전히
올바른, 이미 존재하던) 일반 경로를 탑니다 — 조용히 빠진 기능이 아니라 실제로 문서화된 플랫폼
차이입니다.

**검증**: 실제로 돌아가는 서버에 `strace`를 걸어 syscall이 정말로 발생함을 확인했습니다
(벤치마크의 10,277바이트 파일에 대해 `sendfile(37, 38, [0] => [10277], 10277) = 10277`), 서빙된
파일과 바이트 단위로 `diff`해도 정확히 일치했습니다. 전체 스위트 통과 — Linux 113/113, Windows
109/109(이 개발 머신에서 TLS가 꺼져 있어서 113이 아니라 109 — 기존의, 무관한 공백이며
`CLAUDE.md`의 Phase 12 로그 참고), 두 플랫폼 모두 경고 없이.

## P2 — 요청당 불필요한 스레드 풀 왕복 줄이기 — 완료, 측정됨

P1과 함께 반영되어 한 라운드로 측정했습니다(둘을 떼어놓고 측정하려면 세 번째 벤치마크 실행이
필요한데, 그건 뭔가를 결정하기 위해서가 아니라 순전히 호기심 때문일 뿐입니다):

- `overlay::resolve()`와 `single_file`의 동등한 로직이 이제 요청 자신의 태그 저장소에 결과를
  캐시합니다(`CONCEPTS.md`의 태그 메커니즘 참고), 그 결과를 계산한 특정 확장 인스턴스로
  키를 잡습니다(`extension_registry::dispatch()`는 항상 `wants()`를 호출한 뒤 그 사이에
  아무것도 실행되지 않은 채로 바로 같은 확장의 `handle()`을 호출합니다 — `src/server/
  extension.cpp` 참고 — 그래서 하나의 listener에 여러 `overlay`/`single_file` 인스턴스가
  올라가 있어도 안전합니다). `wants()`가 이미 처리했다고 표시한 요청이 `handle()`에 도달했을 때
  파일시스템을 더 이상 두 번 stat하지 않습니다.
- `file_stream::open()`은 이제 `fopen`과 `ftell`/`fseek`/`ftell` 크기 탐지를 두 번이 아니라
  스레드 풀 왕복 한 번으로 하고, 호출자가 자신의 `stat()`으로 이미 크기를 알고 있을 때 탐지의
  네 syscall을 통째로 건너뛰는 `known_size` 선택 인자를 받습니다 — `overlay`와 `single_file`
  모두 이미 해석해둔 `file_info::size`를 이제 넘겨줍니다.

**P1+P2 종합 결과**: 루프백에서 13.8K → 약 30K req/s, Docker 네트워크 스택 벤치마크에서 약
11K → 약 24K req/s — nhttpd가 양쪽 모두에서 Apache와 동급에 도달했습니다. 아래 P4가 여기서
더 밀어붙였습니다; 최종 종합 표(58K/36K req/s)는 `ReadMe.md`의 벤치마크 섹션을 참고하세요.

## P3 — 코루틴 프레임 할당 처리량(churn) 줄이기 — 시도, 측정, 반려

P0 누수 수정 후엔 프레임이 올바르게 해제되긴 하지만, 여전히 모든 `co_await`가 콜리 프레임을
위해 새로 힙 할당을 합니다(표준 `operator new` 코루틴 할당 경로). 이론: 요청 하나가 `task<T>`를
반환하는 호출을 수십 번 연쇄하므로, 지속 부하에서 실제로 할당자 부담이 될 수 있다는 것이었습니다.

**시도한 것**: 스레드 로컬, 크기별 free-list 할당자(`task_promise_base`에 `operator new`/
`delete`를 얹어 모든 `task<T>` specialization이 공유) — 구현했고, 전체 스위트가 그대로
통과했으며(113/113) 200만 회 await 누수 재현도 여전히 평탄(~0 증가)해서 최소한 *정확하기는*
했습니다. **이 환경에서는 제대로 프로파일링할 수 없었습니다**: 이 머신의 WSL2 커널
(`6.6.87.2-microsoft-standard-WSL2`)에는 `perf`가 붙을 수 있는 커널에 맞는 `linux-tools`
패키지가 없고, 대안인 `strace -c`는 `ptrace` 아래서 침습성이 너무 커서 이 정확한 워크로드의
처리량을 약 20분의 1로 떨어뜨려 — 측정하려던 핫 패스를 전혀 대표하지 못했습니다. 대신 실제
서버의 직접 A/B 벤치마크(이 문서 전체에서 쓴 것과 같은 루프백 wrk 실행)를 증거로 삼았습니다:
**할당자 적용 시 약 31.4K req/s 대 미적용 시 반복 실행에서 약 29.6~31.6K req/s** — 정상적인
실행 간 노이즈 범위 안이고, 측정 가능한 개선이 없었습니다. 돌이켜보면 코드를 쓰기 전에
고려했어야 할 가능성 높은 이유: glibc 2.26+(이 머신은 2.39)가 이미 거의 같은 일(스레드 로컬,
크기별, 락-프리 소규모 할당 재사용)을 하는 스레드별 `tcache`를 내장 고속 경로로 제공하고
있어서 — 그 위에 직접 만든 동등물이 얻을 게 별로 없었고, 게다가 P1이 정적 파일 요청의
`task<T>` 체인을 *더 짧게* 만들어서(sendfile 경로는 스레드-풀-읽기/리액터-쓰기 루프의 await를
통째로 건너뜁니다), 이 항목이 처음 작성됐을 때보다 상각할 게 더 줄었습니다. 성과 없이 유지하는
대신 **반려**했습니다 — `include/nhttp/async/task.hpp`는 P0 수정만 남은 상태로 되돌아갔습니다.
코루틴 프레임 할당자로 실제 이득을 보는 건 일반적으로 알려진 기법이지만, 이 코드베이스의 실제
형태에서 증명된 건 아닙니다; 이론만으로가 아니라 실제 프로파일링 데이터를 낼 수 있는 환경
(이 WSL2 커널이 아니라 네이티브 리눅스 호스트)에서만 다시 시도할 가치가 있습니다.

## P4 — `thread_pool`의 작업 큐 — 완료, 측정됨, 세 번의 반복

이 항목은 원래 "실제로 병목임을 보여주는 프로파일링 데이터 없이는 건드리지 마세요"라고 되어
있었습니다 — 사용자가 그래도 해달라고 요청해서 실행했고, 이 계획 전체에서 가장 교훈적인
항목이 됐습니다: 종이 위에서는 합리적으로 보였던 두 번의 시도가 구현·벤치마크를 거치며 오히려
처리량을 떨어뜨리는 것으로 드러난 뒤에야 세 번째 것이 자리를 잡았습니다. 매 반복마다 벤치마크
전에 먼저 정합성을 검증했습니다(전체 스위트 + ThreadSanitizer, 그리고 새로운
`tests/unit/test_mpmc_queue.cpp` 스트레스 테스트 — 프로듀서 8개 × 컨슈머 4개 × 각 2만 개
항목을, 전체 항목 수보다 일부러 작게 잡은 큐를 통해 흘려보내며 모든 항목이 정확히 한 번씩
전달되는지 확인) — 미묘하게 잘못된 동시성 primitive는 느린 것보다 더 나쁜 결과이기 때문입니다.

**① 깨우기 신호에 `std::counting_semaphore` — 약 2배 느림으로 측정, 반려.** 작업 저장소를
락-프리 bounded MPMC 링 버퍼(`include/nhttp/async/detail/mpmc_queue.hpp`, Dmitry Vyukov의
잘 알려진 설계)로 바꾸고, "워커 하나 깨우기"를 위한 뮤텍스+condvar를 `std::counting_semaphore`로
교체했습니다. 측정 결과 **13.8K → 15.8K req/s** — P1/P2 적용 전보다 거의 나아지지 않았고,
이 설계가 대체하려던 단순한 뮤텍스+`std::queue`가 이미 내던 약 30K에 한참 못 미쳤습니다.
근본 원인: 세마포어의 `acquire()`/`release()` 쌍은 *매 dequeue마다* 지불해야 하는데, 이미
바빠서 다음 작업을 즉시 찾아내는 워커조차 예외가 아닙니다 — 원래 뮤텍스 설계는 "바쁜" 워커가
자기 큐 뮤텍스를 다시 잠그기만 하면서 동기화 오버헤드를 통째로 건너뛸 수 있었는데, 항목별로
계산하는 세마포어는 그럴 수 없습니다. 벤치마크 중 `top`은 유휴 상태 기준선 약 0%에 비해
시스템(커널/syscall) 시간이 58%로 나타났는데 — 요청당 동기화 syscall이 이전보다 훨씬 많다는
것과 일치하며, 바쁜 스핀(이건 user 시간으로 나타났을 것)은 아니었습니다.

**② 락-프리 큐 + 이벤트 전파 전용 뮤텍스/condvar — 최종적으로 자리 잡은 형태, 그러나 그 전에
두 번째 헛발질이 있었습니다.** 실제 push/pop 데이터 경로는 락-프리 링 버퍼를 그대로 두고,
유휴 워커가 잠들고 깨어날 수 있게 해주는 것만을 위한 순수 `std::mutex`/`std::condition_
variable`을 다시 추가했습니다 — 잠들려는 워커와 경쟁하는 push가 절대 유실되지 않도록 표준적인
"락 아래에서 다시 확인하기" 패턴으로 보호합니다. "바쁜" 워커의 루프는 그러면 어떤 동기화
primitive도 건드리지 않는 순수 락-프리 `try_pop()`이 됩니다 — 세마포어 설계 ①이 하지 못했던
바로 그 간극을 메웁니다. 그런데 이 형태의 첫 벤치마크는 `blocking_pool_size = 64`(제네릭
read/write 경로가 요청당 여러 개의 동시 블로킹 훅을 필요로 했을 때 이 계획 자체가 예전에
권장했던 값)에서 **여전히 약 12.5K req/s에 불과했습니다**. 실제 원인: 이 머신의 CPU 코어 8개
위에서 OS 워커 스레드 64개가 경합하는 건 진짜 무거운 오버섭스크립션이고, CAS-재시도-루프
기반 큐는 이 상황에서 심하게 나빠집니다(재시도 도중에 선점된 스레드는 다른 모든 스레드의
재시도 작업을 움직이는 표적으로 만드는 반면, 블로킹 뮤텍스는 그냥 OS가 유휴 스레드를
쉬게 해줍니다) — 직접 확인했습니다: 풀 크기 64에서 약 30K~32K를 기록했던 바로 그
뮤텍스+`std::queue` 설계가 풀 크기 8에서는 약 20K로 떨어졌고(반대 경향), 이 락-프리 설계는
풀 크기 64에서 약 12.5K였다가 **풀 크기 8에서 약 45K, 라이브러리 자체의 손대지 않은
기본값인 4에서 약 69K**까지 올라갔습니다 — P1의 sendfile 경로 덕분에 요청 하나가 이제
이 계획을 처음 작성했을 때 순진한 구현이 필요로 했던 5개 이상이 아니라 스레드 풀 훅을
딱 두 번(stat + open)만 필요로 하기 때문에, 큰 워커 풀이 애초에 필요하지 않았던 것입니다.
**결과적으로 `blocking_pool_size`는 이제 수동 튜닝이 전혀 필요 없습니다 — 배포된 기본값(4)이
측정상 가장 빠른 설정입니다**, 이 계획 자체의 원래 P1 가이드와 정반대입니다.

**③ "누가 대기 중인가" 게이트 제거 — 더 단순함, 측정상 동등함.** ②의 첫 작동 버전은
깨우기(`enqueue()`의 notify)를 원자적 대기자 카운트 뒤에 게이트를 걸어, 아무도 자고 있지 않을
때는 뮤텍스/condvar를 통째로 건너뛰었습니다. 검토하면서 타당한 질문이 나왔습니다: glibc의
`condition_variable::notify_one()`은 이미 내부적으로 대기자를 추적해서 깨울 게 없으면 그 자체로
futex-wake syscall을 건너뛰는데, 이 게이트가 정말 제 몫을 하는가? 그리고 매 `enqueue()`
호출마다 추가되는 원자적 읽기 + 분기가 그 자체로 분기 예측 비용을 유발하지는 않는가? A/B
벤치마크: 게이트 있음 약 66K~69K req/s 대 게이트 없음 약 63K~68K req/s, 반복 실행 기준 —
정상 노이즈 범위 안에서 동등했습니다. 더 단순한 버전을 채택했습니다(`waiting_` 카운터 없이
무조건 `lock_guard` + `notify_one()`) — 원자 변수 하나, 분기 하나가 줄었고 측정된 처리량은
같습니다.

**검증**: 모든 반복에서 Linux 119/119, Windows 115/115(이 개발 머신은 TLS 없음)를 통과했고,
최종 형태에 대해 ThreadSanitizer 아래서 전체 스위트(160,457개 assertion)가 깨끗했습니다 —
락-프리 큐의 CAS 로직에서도, 뮤텍스로 보호되는 깨우기 경로에서도 데이터 레이스가 없었습니다.

## P5 — 윈도우 슬라이딩, 메모리 매핑 파일 읽기 경로 — 완료, 측정됨

`io::file_stream`의 읽기 전용("rb") open은 이제 버퍼링된 `fread()` 대신 파일을 메모리
매핑할 수 있습니다 — `platform::file_mapping`(`include/nhttp/platform/file_mapping.hpp`,
POSIX `mmap`/Windows `CreateFileMapping`+`MapViewOfFile`) 참고.

**제한된 윈도우, 전체 파일 매핑이 아닙니다.** "파일 전체를 매핑"하는 순진한 구현은 명시적으로
배제했습니다: 멀티 기가바이트 파일을 작은 바이트 `Range` 요청 하나(또는 큰 파일의 순차 읽기
한 번)를 위해 통째로 매핑하는 건 잘해야 주소 공간과 페이지 테이블 설정을 낭비하는 것이고,
32비트 대상에서는 아예 주소 공간을 고갈시킬 수도 있습니다. 대신 `file_mapping::
ensure_window(offset, length)`가 파일 위에서 제한된(4&nbsp;MiB) 윈도우 하나를 슬라이딩시키며,
현재 매핑된 범위를 벗어난 요청이 들어올 때만 다시 매핑합니다 — 순차적으로 읽는 경우(일반적인
경우)는 대략 4&nbsp;MiB마다 한 번씩만 재매핑하게 되고, 그보다 작은 파일(실제 정적 파일
대다수, 이 프로젝트 자신의 벤치마크가 쓰는 파일 포함)은 윈도우가 없는 설계와 마찬가지로
파일 전체를 커버하는 윈도우 하나에 매핑됩니다. 직접 검증했습니다: 전용 테스트
(`tests/unit/test_file_stream.cpp`의 "file_stream slides its mapped window correctly across a
file bigger than one window")가 윈도우 3개 조금 넘는 크기의 파일을 만들어 순차 읽기, 이전
윈도우로의 되돌아가는 seek, 윈도우 두 개를 건너뛰는 앞으로의 seek을 모두 바이트 단위로
정확하게 검증합니다.

**지연 생성, 즉시 생성이 아닙니다 — 어렵게 알아냈습니다.** 첫 버전은 `file_stream::open()`
안에서, P2의 기존 결합된 `fopen`+크기-탐지 훅과 나란히 매핑을 즉시 열었습니다. 이건 흔한
경우를 측정 가능하게 *퇴보*시켰습니다: 전체 파일 GET은 `read()`를 아예 호출하지 않는데(P1의
sendfile 경로가 `native_fd()`에서 바로 보냄), 그런 요청 하나하나가 전혀 쓰이지 않을 두 번째
`open()`+`fstat()` 쌍의 비용을 치르고 있었던 것입니다. 벤치마크가 즉시 이를 잡아냈습니다 —
P1의 sendfile 경로가 실제 데이터 전송 측면에서는 완전히 멀쩡하고 영향받지 않았음에도, 처리량이
P1/P2 적용 전 수준(약 10~16K req/s)으로 되돌아갔습니다; 그 쓸모없는 파일 오픈 쌍 하나만으로
그 손실 전부를 설명할 수 있었습니다. 매핑 시도를 `read()` 자체 안으로 옮기고, 스트림당 한 번만
확인하는 `mapping_attempted_` 플래그로 게이트를 걸어 고쳤습니다 — 실제로 `read()`를 호출하는
요청(바이트 `Range`, TLS, `Transfer-Encoding: chunked` 응답 — 전부 sendfile 경로를 타지
않는 것들)만 이 경로에 도달하므로, 전체 파일 GET은 P5가 존재하기 전과 정확히 같은 비용을
치릅니다. 첫 `read()` 이전(즉 매핑을 시도하기 전)에 호출된 `seek()`는 여전히 실제
`fseek()` 기반 경로를 그대로 타고, 지연 매핑 시도는 매핑을 여는 것과 같은 스레드 풀 훅
안에서 `portable_ftell64()`로 `FILE*`의 커서를 읽어오므로, 이전의 어떤 seek이 남겨둔
위치에서든 조용히 0바이트로 리셋되지 않고 올바르게 이어받습니다.

**검증**: 위의 전용 윈도우 테스트를 포함해 각 수정 후 두 플랫폼 모두에서 전체 스위트가
통과했고, 즉시-대-지연 회귀와 그 수정 모두 코드 리뷰만으로 추정하지 않고 이 문서 전체에서
쓴 것과 같은 루프백 `wrk` 벤치마크로 확인했습니다.

## 이 계획에서 명시적으로 제외되는 것

- 이미 별도로 추적 중인 것들: QUIC/HTTP-3(`CLAUDE.md`의 아키텍처 결정 8번 참고),
  TLS 위 ALPN 협상 HTTP/2, HTTP/2 `CONTINUATION`/서버 푸시(`CLAUDE.md`의 Phase 14 로그 참고).
- 리버스 프록시 커넥션 풀링과 능동적 업스트림 헬스체크(`CLAUDE.md`의 Phase 13 로그가 이미 이번
  라운드 범위 밖의 알려진, 합리적인 후속 작업으로 기록해둠).
- `io_context`/`async_socket`을 현재의 이식 가능한 준비성(readiness) 기반 계약 대신 네이티브
  완료 모델 중심으로 다시 짜는 것(`CLAUDE.md`의 Phase 12 설계 노트가 Windows/IOCP에서도 왜
  일부러 이걸 피했는지 설명함) — 여기 어떤 항목도 그걸 재검토할 필요가 없습니다.
